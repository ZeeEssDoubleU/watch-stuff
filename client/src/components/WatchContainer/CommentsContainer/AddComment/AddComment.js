import React from "react";
import PropTypes from "prop-types";

import "./AddComment.scss";

const AddComment = props => {
	return <div className="add-comment">Add comment here.</div>;
};

AddComment.propTypes = {};

export default AddComment;
